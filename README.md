# Aerbloom website - content update

Replace the existing site files in your GitHub repository with the contents of this package.
If your repo already has the assets folder, styles.css, and script.js, you can upload the HTML-only package instead and overwrite the existing HTML files.
